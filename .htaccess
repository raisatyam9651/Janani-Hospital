RewriteEngine On

# Ensure DirectoryIndex is set
DirectoryIndex index.php

# Redirect to remove trailing index.php
RewriteCond %{THE_REQUEST} ^[A-Z]{3,9}\ /+(.*)index\.php [NC]
RewriteRule ^ %1 [R=301,L]

# Custom rewrites for thank you and confirmed pages
RewriteRule ^thank-you/?$ pages/thank-you.php [L]
RewriteRule ^appointment-confirmed/?$ pages/appointment-confirmed.php [L]

# Remove .php extension
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME}.php -f
RewriteRule ^(.*)$ $1.php [L]

# Optional: redirect .html to .php (if users have old bookmarks)
RewriteCond %{THE_REQUEST} ^[A-Z]{3,9}\ /+(.*)\.html [NC]
RewriteRule ^(.*)\.html$ /$1.php [R=301,L]
